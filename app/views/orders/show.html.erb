<div class="container">
  <h4 class="bg-light">注文履歴詳細</h4>
  <div class="row">
    <div class="col">
      注文情報
      <table class="table">
        <tr>
          <td>注文日</td>
          <td><%= @order.created_at.strftime('%Y/%m/%d') %></td>
        </tr>
        <tr>
          <td>配送先</td>
          <td><%= @order.address %></td>
        </tr>
        <tr>
          <td>支払方法</td>
          <td>
            <%= @order.payment_method_i18n %>
          </td>
        </tr>
        <tr>
          <td>ステータス</td>
          <td>ステータス</td>
        </tr>
      </table>
    </div>

    <div class="col">
      請求情報
      <table class="table">
        <tr>
          <td>商品合計</td>
          <td><%= number_with_delimiter((@order.total_payment).round, delimiter: ',') %></td>
        </tr>
        <tr>
          <td>配送料</td>
          <td><%= @shipping_cost %></td>
        </tr>
        <tr>
          <td>ご請求額</td>
          <td><%= number_with_delimiter((@order.total_payment + @shipping_cost).round, delimiter: ',') %></td>
        </tr>
      </table>
    </div>
  </div>

  <div class="row">
    <div>
      注文内容
      </th>
        <th>単価(税込)</th>
        <th>個数</th>
        <th>小計</th>
      </tr>

      <% @order.order_details.each do |order_detail| %>
        <tr>
          <td><%= order_detail.item.name %></td>
          <td><%= order_detail.price %></td>
          <td><%= order_detail.amount %></td>
          <td><%=<table class="table">
      <tr>
        <th>商品 order_detail.price * order_detail.amount %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>